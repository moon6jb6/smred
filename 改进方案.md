# 赓续红色血脉 — 改进方案

> 董事会三轮审议，全票通过。
> 核心理念：**这不是一个红色网站，这是一台时间机器。**

---

## 一、创作说明

六个模块，六种穿越方式——走过长征路、触摸历史物件、在黑暗中发现英雄、面对改变命运的选择、指挥一场战役、看见中国力量。每一个模块的终点不是"了解了"，而是"忘不了"。

**反共识：** 别人在"展示红色文化"，我们在"让用户感受红色文化"。感受 > 了解。

---

## 二、模块架构（6个模块 + 首页）

### 首页

- 全屏黑 1 秒 → 钩子文字 **"如果你能穿越回过去，你最想回到哪一年？"** → 停留 2 秒 → 淡出
- 浮现一条从左下到右上的土路（CSS手绘/图片），路旁 6 个路标
- 路起点标注"1840"，终点标注"2025"
- 鼠标悬停路标：路标亮起，显示模块名+一句话预览
- 点击路标：胶片转场（旧电影黑白条纹闪过，0.8s）进入模块
- 模块返回首页：同样的胶片转场

### mod1 重走长征路

| 项目 | 内容 |
|------|------|
| 核心交互 | 全屏滚动叙事 + 密度节奏控制 |
| 情感目标 | 感受"长征的重量" |
| 技术要点 | CSS scroll-snap + IntersectionObserver |

- 每个阶段占满一屏，CSS scroll-snap-type 做整屏吸附
- 每阶段内容密度不同：草地段落密集（用户自然停留久=节奏慢），突围段落简洁（用户快速掠过=节奏快）
- 湘江阶段：数字 86000 → 30000（requestAnimationFrame 递减），页面轻微震动（CSS transform translateX 随机偏移 2px，持续 2s），然后强制停顿——禁用滚动 2 秒，屏幕只显示大字"30000"
- 背景色随阶段渐变（CSS transition on background-color）
- 每阶段有历史照片 + 文字，图片用 loading="lazy"
- 保底版本：去掉滚动吸附，改为普通滚动 + 淡入动画

### mod2 象棋之战

| 项目 | 内容 |
|------|------|
| 核心交互 | 零干扰对弈 + 终局一击 |
| 情感目标 | 感受"战争的代价" |
| 技术要点 | 现有棋盘逻辑 + 终局触发器 |

- 保留现有残局系统、棋盘逻辑、关卡选择
- 每个关卡标题显示绑定的战役名称
- 下棋过程中零文字弹出、零干扰
- 终局触发：
  - 赢了 → 屏幕渐暗（opacity transition 1.5s），3s 后文字浮现："XX战役，红军赢了。代价：XXXX人。" 5s 后渐隐，显示"下一关"
  - 输了 → 屏幕渐暗，文字："历史没有重来。" 3s 后显示"再试一次"
- 棋盘底图：该战役的地形手绘风格（CSS 等高线或简单底图）
- 保底版本：只加终局文字弹出，不改底图

### mod3 时间博物馆

| 项目 | 内容 |
|------|------|
| 核心交互 | 历史物件自由探索 + 收集计数 |
| 情感目标 | 感受"历史的温度" |
| 技术要点 | CSS transition + 点击展开 + 绝对定位 |

- 页面散落 10 个历史物件（绝对定位，环形大致排列，每个偏移 ±10 度）
- 物件清单：南京条约（折纸）、太平天国旗帜、戊戌变法奏折、辛亥革命手枪、五四传单、中共一大会议桌、南昌起义军旗、遵义会议桌、开国大典话筒、改革开放红头文件
- 悬停：物件浮起（scale 1.1 + translateY -10px），标签展开一句话描述
- 点击：物件放大到屏幕中央（position fixed + transition），背景变暗，展开 200 字故事 + "放回"按钮
- 底部统计："你已探索 X/10 个历史瞬间"，全部探索完解锁总结语
- 和 mod1 的差异：mod1 是线性叙事（我带你走），mod3 是自由探索（你自己逛）
- 保底版本：物件改为文字卡片，不用图片

### mod4 祭祀大典

| 项目 | 内容 |
|------|------|
| 核心交互 | 星空微光 + 真实语录 + 蜡烛 |
| 情感目标 | 感受"铭记的重量" |
| 技术要点 | 鼠标追踪 + CSS光晕 + localStorage |

- 全黑背景，12 颗星散布（CSS 绝对定位）
- document.onmousemove 追踪鼠标，鼠标周围 200px 半径渐变光晕（radial-gradient）
- 星星靠近时亮起（CSS transition），显示人物剪影 + 名字 + 生卒年
- 点击星星：弹出全屏暗色浮层，内容为该人物的**真实原话**（语录、日记、采访原句），附出处
- 浮层底部蜡烛按钮，点击后 CSS 火焰动画（纯 CSS），存入 localStorage
- 再次访问：之前点亮的蜡烛自动亮着，星星从冷色变暖色
- 技术栈：纯 HTML/CSS/JS，无外部依赖
- 保底版本：去掉光晕效果，改为普通悬停高亮

### mod5 毛泽东——三次选择

| 项目 | 内容 |
|------|------|
| 核心交互 | 三次选择卡片翻转 |
| 情感目标 | 感受"一个人的分量" |
| 技术要点 | CSS 3D flip |

- 页面只有三张大卡片，横排（移动端纵排）
- 卡片一：1927年，秋收起义——"他选择了农村，而不是城市"
- 卡片二：1935年，遵义会议——"他选择了实事求是，而不是教条"
- 卡片三：1949年，天安门——"他选择了宣告，而不是沉默"
- 点击卡片：CSS 3D flip 翻转，背面显示：历史背景（100字）+ 本人原话 + 关键数据
- 底部一句话："一个人的三次选择，改变了一个国家的命运。"
- 备选方案（史料不足时）：改为"毛泽东诗词地图"——6首诗词对应6个地点
- 保底版本：不翻转，改为点击展开文字

### mod6 中国力量（合并原 mod7 + mod8 + mod9）

| 项目 | 内容 |
|------|------|
| 核心交互 | 数据线 + 对比表 + 关系地图 |
| 情感目标 | 感受"中国的力量" |
| 技术要点 | SVG + requestAnimationFrame |

- 页面分三个区块，纵向滚动：
- **区块一：一条线**（数据叙事）
  - 核心数据：高铁里程 0 → 45000 公里，SVG 线随滚动进度增长
  - 线上 5 个节点，hover 弹出该段故事（建设者名字、时长、一句话）
- **区块二：一张表**（对比叙事）
  - 清朝火绳枪 vs 95式步枪，3 组数据并排
  - 数据用 requestAnimationFrame 递增动画
  - 每组对比下面一句话
- **区块三：一张图**（关系叙事）
  - 中国为中心的平面地图，6 条 SVG 关系线辐射
  - 线粗细 = 贸易额，hover 展开一句话 + 数据
  - 点击展开 100 字故事
- 保底版本：去掉动画，改为静态数据+文字

---

## 三、执行顺序

| 顺序 | 模块 | 预计工期 | 优先级理由 |
|------|------|---------|-----------|
| 1 | mod4 祭祀大典 | 2-3天 | 风险最低，技术简单，情感确定性高 |
| 2 | mod1 长征路 | 3-4天 | 中等难度，滚动吸附需调试 |
| 3 | mod3 时间博物馆 | 2-3天 | 内容整理为主，技术不复杂 |
| 4 | mod5 毛泽东 | 2天 | 卡片翻转简单，需找史料 |
| 5 | mod2 象棋改造 | 2天 | 在现有代码上改，加终局逻辑 |
| 6 | mod6 中国力量 | 4-5天 | 最复杂，三区块+SVG |
| 7 | 首页重做 | 2天 | 需要所有模块定稿后再做 |

**总计：约 17-21 天**

---

## 四、技术规范

- **技术栈：** 纯 HTML/CSS/JS，不引入任何外部框架/库
- **外部依赖唯一例外：** Font Awesome（图标），已在使用
- **禁止事项：** 做的过程中不新增功能，不引入 Three.js/GSAP 等外部库
- **图片策略：** 首屏图片 loading="eager"，其余 loading="lazy"
- **存储：** 用户状态（蜡烛、探索进度）用 localStorage
- **响应式：** 所有模块覆盖 768px / 480px 断点
- **保底原则：** 每个模块先做到保底版本可运行，再提升到理想版本

---

## 五、模块间导航

- 首页 → 任意模块（路标点击 + 胶片转场）
- 每个模块底部 → "下一个穿越"按钮（按叙事顺序）+ "返回首页"按钮
- 叙事顺序：mod1 → mod3 → mod4 → mod5 → mod6 → mod2 → 首页
- 转场统一：进入/退出模块都用胶片转场（旧电影黑白条纹闪过，0.8s）
